pipeline {
    agent any
    environment {
        name = "Naani"
        course = "K8S"
    }
    stages {
        stage ('Build') {
            steps {
                echo "Welcome ${name}"
                echo "You enrolled ${course} Course"
                //echo "You are certified in GCP"
            }
        }
    }
}
